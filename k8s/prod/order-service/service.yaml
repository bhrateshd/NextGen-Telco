apiVersion: v1
kind: Service
metadata:
  name: order-service
  labels:
    app: order-service
    env: production
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
